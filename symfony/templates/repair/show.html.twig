{% extends 'base.html.twig' %}

{% block title %}Repair{% endblock %}

{% block body %}
    <h1>Repair</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ repair.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ repair.name }}</td>
            </tr>
            <tr>
                <th>CarId</th>
                <td>{{ repair.carId }}</td>
            </tr>
            <tr>
                <th>UserId</th>
                <td>{{ repair.userId }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_repair_index') }}">back to list</a>

    <a href="{{ path('app_repair_edit', {'id': repair.id}) }}">edit</a>

    {{ include('repair/_delete_form.html.twig') }}
{% endblock %}
